[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "zalkanes node status"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "zalkanes"

[services.variables]
RUST_LOG = "zalkanes=info"
ZALKANES_NETWORK = "regtest"
